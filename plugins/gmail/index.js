/** gmail plugin — Lead capture email
 * actions: connect, configure, invoke
 * triggers: manual, event
 */
export const name = "gmail";
export function register(ctx) { ctx.log("plugin gmail registered"); }
