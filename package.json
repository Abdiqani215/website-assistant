{
  "name": "website-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Site-embedded assistant answering from the site's own content and routing to pages or contact.",
  "scripts": {
    "dev": "node apps/api/server.js",
    "start": "node apps/api/server.js",
    "build": "echo 'build step'",
    "test": "node --test tests/",
    "deploy:cf": "wrangler deploy",
    "deploy:docker": "docker compose up --build -d"
  },
  "engines": {
    "node": ">=18"
  }
}